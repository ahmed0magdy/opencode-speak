{
  "hooks": {
    "Stop": [
      {
        "command": "bash \"${PLUGIN_ROOT}/hooks/tts-stop.sh\" \"${transcript_path}\"",
        "description": "Speak assistant response via TTS"
      }
    ]
  }
}
